<br>
<select name="answer[<%= survey_id %>][<%= question.id %>]" >
  <% question.question_options.each do |option| %>
      <option value="<%= option.select_option_value %>" > <%= option.option_text %> </option>
  <% end %>
</select>