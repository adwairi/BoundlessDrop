<% question.question_options.each do |option| %>
    <input type="radio" name="answer[<%= survey_id %>][<%= question.id %>]" value="<%= option.select_option_value %>" /> <%= option.option_text %> <br />
<% end %>

